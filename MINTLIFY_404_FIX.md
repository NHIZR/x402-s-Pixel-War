# Mintlify "Page Not Found" 问题修复

## 🔍 问题现状

访问 https://x402spixelwar.mintlify.app/introduction 显示 **"Page Not Found"**

## 原因分析

虽然：
- ✅ GitHub 仓库中文件存在
- ✅ mint.json 配置正确
- ✅ 文件语法没有错误
- ✅ 网站可以访问（返回 200）

但是：
- ❌ Mintlify 没有找到文档内容
- ❌ 可能是 Dashboard 配置问题

## 🔧 解决方案

### 方案 1: 检查 Mintlify Dashboard 配置

1. **访问 Dashboard**
   https://dashboard.mintlify.com/x402spixelwar/x402spixelwar

2. **检查 Settings → Repository**
   确认以下设置：

   | 设置项 | 应该是 | 说明 |
   |--------|--------|------|
   | **Repository** | `NHIZR/x402-s-Pixel-War` | ✅ 您的仓库 |
   | **Branch** | `main` | ⚠️ 必须是 main |
   | **Root Directory** | `/` (空或根目录) | ⚠️ 不要填子目录 |

3. **检查 Deployments**
   - 点击 "Deployments" 标签
   - 查看最新构建状态
   - 如果失败，点击查看错误日志

### 方案 2: 手动触发重新部署

1. 在 Dashboard → Settings
2. 找到 "Redeploy" 或 "Trigger Deployment" 按钮
3. 点击触发新构建
4. 等待 2-3 分钟

### 方案 3: 重新连接仓库

如果上述方法都不行：

1. **断开连接**
   - Settings → Repository
   - 点击 "Disconnect" 或 "Remove"

2. **重新连接**
   - 点击 "Connect GitHub Repository"
   - 选择 `NHIZR/x402-s-Pixel-War`
   - **Branch**: 选择 `main`
   - **Root Directory**: 留空（或填 `/`）
   - 保存

3. **等待自动部署**

### 方案 4: 验证 mint.json 路径

检查 `mint.json` 中的页面路径是否正确：

```bash
# 在本地运行
cat mint.json | grep -A 20 '"navigation"'
```

应该看到：
```json
"navigation": [
  {
    "group": "Getting Started",
    "pages": [
      "introduction",      // ← 不是 "introduction.mdx"
      "quickstart"         // ← 不是 "quickstart.mdx"
    ]
  }
]
```

**注意**：路径中**不要包含 .mdx 扩展名**！

### 方案 5: 尝试访问其他 URL

测试以下 URL 看哪些能访问：

```
# 首页
https://x402spixelwar.mintlify.app/

# Introduction（不带 /introduction）
https://x402spixelwar.mintlify.app/

# Quick Start
https://x402spixelwar.mintlify.app/quickstart

# API Reference
https://x402spixelwar.mintlify.app/api-reference/introduction
```

告诉我哪个能访问，哪个不能访问。

---

## 🐛 常见错误

### 错误 1: Root Directory 设置错误

**症状**: 所有页面都 404

**原因**: Root Directory 设置成了子目录（如 `/docs` 或 `/mintlify-docs`）

**解决**: 设置为 `/` 或留空

### 错误 2: Branch 选择错误

**症状**: 显示旧内容或 404

**原因**: 选择了 `mintlify/connect-github-repository-69082` 而不是 `main`

**解决**: 切换到 `main` 分支

### 错误 3: 路径包含 .mdx 扩展名

**症状**: 特定页面 404

**原因**: `mint.json` 中写了 `"introduction.mdx"` 而不是 `"introduction"`

**解决**: 移除 `.mdx` 扩展名

### 错误 4: 构建失败但没有提示

**症状**: Dashboard 显示构建成功，但页面 404

**原因**: MDX 语法错误，Mintlify 静默失败

**解决**: 检查 MDX 文件语法

---

## 📋 调试清单

请按顺序检查：

- [ ] Dashboard → Settings → Branch 是否为 `main`
- [ ] Dashboard → Settings → Root Directory 是否为 `/` 或空
- [ ] Dashboard → Deployments → 最新构建是否成功
- [ ] 如果构建失败，查看错误日志
- [ ] 尝试手动触发 Redeploy
- [ ] 检查 `mint.json` 中路径没有 `.mdx`
- [ ] 尝试访问根路径 `/` 看是否有任何内容
- [ ] 如果实在不行，重新连接仓库

---

## 🆘 如果还是不行

### 获取详细信息

1. **截图 Dashboard 的 Settings 页面**
   - Repository 设置
   - Branch 设置
   - Root Directory 设置

2. **截图 Deployments 页面**
   - 最新构建状态
   - 如果有错误日志，复制文字

3. **告诉我哪些 URL 能访问**
   ```
   / - ？
   /introduction - ？
   /quickstart - ？
   /api-reference/introduction - ？
   ```

4. **检查浏览器控制台**
   - 按 F12 打开开发者工具
   - 切换到 Console 标签
   - 看是否有错误信息
   - 截图或复制错误

---

## 💡 临时解决方案：本地预览

如果在线版本一直有问题，可以先用本地预览：

```bash
# 安装 Mintlify CLI
npm install -g mintlify

# 在项目目录运行
cd "/Users/lobesterk/Library/Mobile Documents/com~apple~CloudDocs/x402's Pixel War"

# 启动本地服务器
mintlify dev

# 访问 http://localhost:3000
```

本地预览可以帮助确认：
- 文件是否能正常渲染
- MDX 语法是否有错误
- 导航是否正确

---

## 🎯 下一步

1. **立即操作**：
   - 访问 Dashboard 检查 Settings
   - 手动触发 Redeploy
   - 等待 2-3 分钟

2. **如果还是 404**：
   - 截图 Dashboard 设置
   - 告诉我构建日志中的错误
   - 我帮您继续诊断

3. **最坏情况**：
   - 重新创建一个新的 Mintlify 项目
   - 或使用其他文档平台（Docusaurus, GitBook）

---

**最后更新**: 2026-01-23
**状态**: 诊断中，等待用户反馈
