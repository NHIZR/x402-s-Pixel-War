# 项目文件结构

这个文件记录了项目的完整文件结构，用于开发参考。

## 📂 根目录文件

```
x402's Pixel War/
├── .DS_Store              # macOS 系统文件（自动生成）
├── .env.local             # 环境变量（包含密钥，不提交到 Git）
├── .env.local.example     # 环境变量模板
├── .eslintrc.json         # ESLint 配置
├── .gitignore             # Git 忽略文件配置
├── README.md              # 项目主文档
├── next-env.d.ts          # Next.js TypeScript 定义（自动生成）
├── next.config.ts         # Next.js 配置
├── package.json           # npm 依赖配置
├── package-lock.json      # npm 依赖锁定（自动生成）
├── postcss.config.mjs     # PostCSS 配置
├── tailwind.config.ts     # Tailwind CSS 配置
└── tsconfig.json          # TypeScript 配置
```

## 📁 核心目录

### `/app` - Next.js App Router
```
app/
├── layout.tsx             # 根布局（Solana Provider）
├── page.tsx               # 主页（像素网格）
├── globals.css            # 全局样式
└── favicon.ico            # 网站图标
```

### `/components` - React 组件
```
components/
├── game/                  # 游戏组件
│   ├── Grid.tsx          # 主网格组件
│   ├── Pixel.tsx         # 单个像素
│   ├── UserInfo.tsx      # 用户信息和钱包连接
│   ├── PixelInfoModal.tsx       # 单个像素占领弹窗
│   ├── BatchConquerModal.tsx    # 批量占领弹窗
│   └── ColorPicker.tsx          # 颜色选择器
├── providers/
│   └── SolanaWalletProvider.tsx # Solana 钱包 Provider
└── ui/                    # shadcn/ui 组件
    ├── button.tsx
    ├── dialog.tsx
    └── ...
```

### `/lib` - 工具库和逻辑
```
lib/
├── solana/
│   ├── balance.ts        # USDC 余额查询
│   └── mockPayment.ts    # Mock x402 支付系统 (Day 1) ✅
├── supabase/
│   ├── client.ts         # Supabase 浏览器客户端
│   └── server.ts         # Supabase 服务端客户端
├── services/
│   └── pixelConquest.ts  # 像素占领服务层 (Day 1) ✅
├── stores/
│   ├── gameStore.ts      # 游戏状态（Zustand）
│   └── userStore.ts      # 用户状态（Zustand）
├── hooks/
│   └── useRealtime.ts    # Supabase 实时订阅
├── types/
│   └── game.types.ts     # TypeScript 类型定义
└── utils/
    ├── priceCalculation.ts  # 价格计算工具
    ├── cn.ts                # className 工具
    └── ...
```

### `/supabase` - 数据库
```
supabase/
├── schema.sql                  # 完整数据库架构
├── schema-guest-mode.sql       # 游客模式架构（简化版）
└── schema-wallet-bridge.sql    # 钱包桥接函数 (Day 1) ✅
```

### `/scripts` - 工具脚本
```
scripts/
├── check-wallet.ts       # 钱包检查工具
├── setup-test-usdc.sh    # 测试 USDC 设置脚本
└── test-conquest.ts      # 占领功能测试脚本 (Day 1) ✅
```

### `/docs` - 文档 (已优化整合)
```
docs/
├── README.md                   # 文档索引 v2.0
├── GETTING_STARTED.md          # 快速开始指南（整合版）⭐
├── SETUP_DATABASE.md           # 数据库设置指南 ✅
├── HACKATHON_SPRINT.md         # 黑客松 5 天计划
├── DAY1_PROGRESS.md            # Day 1 进度报告 ✅
├── OPTIMIZATIONS.md            # 性能优化总结 ⚡
└── screenshots/                # 截图目录
    └── wallet-connected.png

# 已整合到 GETTING_STARTED.md:
#   - DEVELOPMENT.md (开发指南)
#   - WALLET_SETUP.md (钱包设置)
#   - HOW_TO_GET_DEVNET_USDC.md (测试代币)
```
docs/
├── README.md                   # 文档索引
├── WALLET_SETUP.md             # 钱包设置指南
├── HOW_TO_GET_DEVNET_USDC.md  # 获取测试代币指南
├── DEVELOPMENT.md              # 开发指南
├── SETUP_DATABASE.md           # 数据库设置指南 (Day 1) ✅
├── HACKATHON_SPRINT.md         # 黑客松 5 天计划
├── DAY1_PROGRESS.md            # Day 1 进度报告 (Day 1) ✅
├── OPTIMIZATIONS.md            # 性能优化总结 ⚡
└── screenshots/                # 截图目录
    └── wallet-connected.png
```

### `/public` - 静态资源
```
public/
├── next.svg
├── vercel.svg
└── ...
```

## 🚫 忽略的目录

这些目录不应该提交到 Git:

- `node_modules/` - npm 依赖（通过 package.json 安装）
- `.next/` - Next.js 构建输出（自动生成）
- `.claude/` - Claude Code 工作目录（自动生成）
- `out/` - 静态导出目录（如果使用）
- `.env.local` - 环境变量（包含密钥）

## 📦 打包和构建

### 开发模式
```bash
npm run dev
# 启动 Next.js 开发服务器
# 输出在内存中，不生成文件
```

### 生产构建
```bash
npm run build
# 生成 .next/ 目录
# 包含优化后的生产代码
```

### 生产运行
```bash
npm start
# 运行 .next/ 目录中的代码
```

## 🔄 自动生成的文件

这些文件由工具自动生成，不应手动编辑:

- `next-env.d.ts` - Next.js TypeScript 定义
- `package-lock.json` - npm 依赖锁定
- `.next/` - Next.js 构建输出
- `node_modules/` - npm 依赖

## 📝 配置文件说明

### `package.json`
- 项目元数据
- npm 脚本定义
- 依赖声明

### `tsconfig.json`
- TypeScript 编译器配置
- 路径别名 (@/ → 根目录)

### `tailwind.config.ts`
- Tailwind CSS 配置
- 自定义颜色和主题
- 内容扫描路径

### `next.config.ts`
- Next.js 框架配置
- 图片优化
- 环境变量

### `.eslintrc.json`
- 代码风格规则
- Next.js 推荐配置

## 🔍 查找文件技巧

### 按功能查找
- **钱包相关**: `components/game/UserInfo.tsx`, `lib/solana/`
- **像素网格**: `components/game/Grid.tsx`, `components/game/Pixel.tsx`
- **占领逻辑**: `components/game/*Modal.tsx`, `lib/hooks/`
- **状态管理**: `lib/stores/`
- **数据库**: `supabase/`, `lib/supabase/`

### 按类型查找
- **组件**: `components/`
- **页面**: `app/`
- **类型定义**: `lib/types/`
- **工具函数**: `lib/utils/`
- **样式**: `app/globals.css`, `tailwind.config.ts`

---

最后更新: 2026-01-22
