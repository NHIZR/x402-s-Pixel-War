# 📁 Pixel War 项目结构

完整的项目架构、目录结构和技术说明。

---

## 📊 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户界面 (Frontend)                       │
├─────────────────────────────────────────────────────────────────┤
│  Next.js 15 + React 19 + Tailwind CSS + Zustand                │
│  ├── Grid.tsx (64×36 像素网格，共 2304 个像素)                  │
│  ├── PixelInfoModal / BatchConquerModal (占领弹窗)              │
│  ├── UserInfo / TransactionPanel (用户信息+交易记录)            │
│  └── SolanaWalletProvider (钱包连接)                            │
└───────────────────┬─────────────────────┬───────────────────────┘
                    │                     │
                    ▼                     ▼
┌───────────────────────────┐  ┌──────────────────────────────────┐
│     Supabase 后端          │  │       Solana 区块链               │
├───────────────────────────┤  ├──────────────────────────────────┤
│ • PostgreSQL 数据库        │  │ • USDC SPL Token 支付             │
│ • Realtime WebSocket      │  │ • 钱包签名验证                    │
│ • RPC 函数 (像素操作)      │  │ • Devnet / Mainnet               │
│ • 行级安全 (RLS)          │  │ • Helius RPC                     │
└───────────────────────────┘  └──────────────────────────────────┘
```

---

## 🗂️ 目录结构

```
pixel-war/
├── 📱 app/                          # Next.js App Router 应用
│   ├── api/
│   │   └── faucet/
│   │       └── route.ts             # Faucet API - 发放测试 USDC
│   ├── debug/
│   │   └── page.tsx                 # 调试页面
│   ├── globals.css                  # 全局样式
│   ├── layout.tsx                   # 根布局 (Provider 包装)
│   └── page.tsx                     # 主游戏页面
│
├── 🎨 components/                   # React UI 组件库
│   ├── game/                        # 游戏核心组件
│   │   ├── Grid.tsx                 # 主网格容器 (351 行)
│   │   ├── Pixel.tsx                # 单个像素组件 (React.memo 优化)
│   │   ├── PixelInfoModal.tsx       # 像素详情弹窗 (310 行)
│   │   ├── BatchConquerModal.tsx    # 批量占领弹窗 (361 行)
│   │   ├── ColorPicker.tsx          # 颜色选择器 (196 行)
│   │   ├── TransactionPanel.tsx     # 交易面板 (255 行)
│   │   └── UserInfo.tsx             # 用户信息栏 (232 行)
│   ├── providers/
│   │   └── SolanaWalletProvider.tsx # Solana 钱包提供器
│   ├── ui/                          # shadcn/ui 基础组件
│   │   ├── button.tsx
│   │   ├── dialog.tsx               # 对话框组件
│   │   ├── loading.tsx              # 加载界面
│   │   └── toaster.tsx              # Toast 通知
│   ├── FaucetButton.tsx             # Faucet 按钮组件 (136 行)
│   ├── WalletConnectionGuide.tsx    # 钱包连接指南 (93 行)
│   └── ErrorBoundary.tsx            # 错误边界 (81 行)
│
├── 📚 lib/                          # 核心业务逻辑库
│   ├── config/
│   │   └── solana.ts                # Solana 网络配置
│   ├── constants/
│   │   └── game.ts                  # 游戏常量
│   ├── hooks/
│   │   └── usePixelConquest.ts      # 自定义占领 Hook
│   ├── i18n/                        # 国际化
│   │   ├── index.ts                 # i18n 初始化
│   │   ├── translations.ts          # 翻译文本 (中/英)
│   │   └── useLanguage.ts           # 语言切换 Hook
│   ├── services/                    # 业务服务
│   │   ├── pixelConquest.ts         # 像素占领核心服务
│   │   ├── x402Payment.ts           # USDC 支付服务
│   │   └── faucet.ts                # Faucet 服务
│   ├── solana/
│   │   └── balance.ts               # Solana 余额查询
│   ├── stores/                      # Zustand 状态管理
│   │   ├── gameStore.ts             # 游戏状态
│   │   ├── transactionStore.ts      # 交易状态
│   │   └── userStore.ts             # 用户状态
│   ├── supabase/
│   │   ├── client.ts                # Supabase 浏览器客户端
│   │   └── server.ts                # Supabase 服务端客户端
│   ├── types/
│   │   └── game.types.ts            # TypeScript 类型定义
│   ├── utils/
│   │   ├── priceCalculation.ts      # 价格计算工具
│   │   └── rateLimit.ts             # 速率限制工具
│   └── utils.ts                     # 通用工具函数
│
├── 🛠️ scripts/                      # 实用工具脚本
│   ├── create-devnet-token.ts       # 创建 Devnet USDC Token
│   ├── setup-treasury.ts            # 初始化 Treasury 钱包
│   ├── setup-faucet.ts              # 配置 Faucet 钱包
│   ├── check-sol-balance.ts         # 查询 SOL 余额
│   └── README.md                    # 脚本文档
│
├── 📖 docs/                         # 项目文档
│   ├── ARCHITECTURE.md              # 架构设计文档
│   ├── HACKATHON.md                 # 完整功能文档
│   ├── API.md                       # API 文档
│   ├── TESTNET_SETUP.md             # Testnet 配置指南
│   ├── X402_INTEGRATION_GUIDE.md    # x402 集成指南
│   ├── DEPLOYMENT.md                # 部署指南
│   ├── DOCS_INDEX.md                # 文档索引
│   └── docs-mintlify/               # Mintlify 文档 (可选)
│
├── 🗄️ supabase/                     # Supabase 数据库配置
│   ├── schema-complete.sql          # 完整 PostgreSQL Schema
│   ├── migrations/
│   │   └── resize_grid_64x36.sql    # 网格大小迁移
│   ├── archive/                     # 历史 Schema 版本
│   └── README.md                    # Supabase 说明
│
├── 🔐 .private/                     # 私有文件 (不提交 Git)
│   └── wallets/
│       ├── faucet-wallet.json       # Faucet 钱包密钥
│       └── treasury-wallet.json     # Treasury 钱包密钥
│
├── ⚙️ 配置文件
│   ├── package.json                 # NPM 依赖和脚本
│   ├── tsconfig.json                # TypeScript 配置
│   ├── tailwind.config.ts           # Tailwind CSS 配置
│   ├── next.config.ts               # Next.js 配置
│   ├── postcss.config.mjs           # PostCSS 配置
│   ├── .env.local                   # 环境变量 (不提交)
│   ├── .env.example                 # 环境变量模板
│   ├── .gitignore                   # Git 忽略规则
│   ├── vercel.json                  # Vercel 部署配置
│   └── eslint.config.mjs            # ESLint 配置
│
└── 📄 README.md                     # 项目主文档
```

---

## 🔧 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| **前端框架** | Next.js | 15.x |
| **UI 库** | React | 19.x |
| **样式** | Tailwind CSS | 3.4 |
| **状态管理** | Zustand | 5.0 |
| **数据库** | Supabase (PostgreSQL) | - |
| **实时同步** | Supabase Realtime | - |
| **区块链** | Solana | Devnet/Mainnet |
| **支付** | USDC SPL Token | - |
| **钱包** | Solana Wallet Adapter | 0.15.x |
| **部署** | Vercel | - |
| **类型检查** | TypeScript | 5.x |

---

## 🎨 组件层级关系

```
App Layout (app/layout.tsx)
└── SolanaWalletProvider
    └── Page (app/page.tsx)
        ├── Grid
        │   ├── UserInfo (用户钱包和余额显示)
        │   │   ├── WalletButton
        │   │   ├── FaucetButton
        │   │   └── BalanceDisplay
        │   ├── Pixel × 2304 (64×36 网格)
        │   │   └── Pixel (单个像素，React.memo 优化)
        │   ├── PixelInfoModal (像素详情弹窗)
        │   │   ├── ColorPicker
        │   │   └── ConquerButton
        │   └── BatchConquerModal (批量占领弹窗)
        │       ├── ColorPicker
        │       └── BatchConquerButton
        └── TransactionPanel (右侧交易面板)
            ├── TransactionList
            └── UserStats
```

---

## 🗄️ 数据库设计

### 核心表结构

```sql
pixels 表
├── id (UUID)                    -- 主键
├── x, y (INTEGER)               -- 坐标 (0-63, 0-35)
├── color (VARCHAR)              -- HEX 颜色代码
├── current_price (DECIMAL)      -- 当前价格 (USDC)
├── wallet_owner (VARCHAR)       -- 所有者钱包地址
├── conquest_count (INTEGER)     -- 占领次数
├── last_conquered_at (TIMESTAMP)-- 最后占领时间
├── created_at (TIMESTAMP)       -- 创建时间
└── updated_at (TIMESTAMP)       -- 更新时间
```

### RPC 函数

| 函数 | 功能 |
|------|------|
| `get_grid_state()` | 获取完整网格状态 |
| `conquer_pixel_wallet()` | 单像素占领 |
| `conquer_pixels_batch()` | 批量占领 |
| `recolor_pixel_wallet()` | 重新着色 (免费) |
| `recolor_pixels_batch()` | 批量重新着色 |
| `get_wallet_pixels()` | 获取钱包拥有的像素 |

---

## 💰 支付流程

```
用户点击占领
     ↓
Grid 组件捕获点击 → 打开 PixelInfoModal
     ↓
用户选择颜色并确认占领
     ↓
调用 pixelConquest.ts 的 conquerPixel()
     ↓
┌────────────────────────────────────────────┐
│ 支付阶段: processPayment()                  │
├────────────────────────────────────────────┤
│ 1. 验证钱包和金额                          │
│ 2. 获取 USDC Token 账户                    │
│ 3. 创建 SPL Token 转账指令                 │
│ 4. 构建交易                                │
│ 5. 用户签名 (Phantom 钱包弹窗)             │
│ 6. 广播交易到 Solana 网络                  │
│ 7. 等待确认                                │
│ 8. 返回交易哈希                            │
└────────────────────────────────────────────┘
     ↓
┌────────────────────────────────────────────┐
│ 数据更新阶段: Supabase RPC                  │
├────────────────────────────────────────────┤
│ 1. 调用 conquer_pixel_wallet() RPC         │
│ 2. 获取像素行级锁                          │
│ 3. 验证所有权 (已拥有则跳过)               │
│ 4. 计算新价格 (old_price × 1.2)            │
│ 5. 更新 pixels 表                          │
│ 6. 返回结果 JSON                           │
└────────────────────────────────────────────┘
     ↓
┌────────────────────────────────────────────┐
│ 实时同步阶段: Supabase Realtime            │
├────────────────────────────────────────────┤
│ 1. 数据库检测到 UPDATE 事件                │
│ 2. Realtime 服务推送 WebSocket 消息        │
│ 3. 所有连接的客户端接收更新                │
│ 4. Grid 组件更新本地状态                   │
│ 5. React 重新渲染受影响的像素              │
└────────────────────────────────────────────┘
     ↓
游戏 UI 实时更新 (延迟 < 500ms)
```

---

## 🔐 环境变量

### 必需配置 (Vercel 部署)

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx

# Solana
NEXT_PUBLIC_SOLANA_NETWORK=devnet
NEXT_PUBLIC_SOLANA_RPC_URL=https://devnet.helius-rpc.com/?api-key=xxx
HELIUS_API_KEY=xxx

# Token
NEXT_PUBLIC_USDC_MINT_ADDRESS=xxx

# Treasury (接收游戏收入)
NEXT_PUBLIC_GAME_TREASURY_WALLET=xxx
TREASURY_WALLET_PRIVATE_KEY=xxx  # Base58 格式，仅服务端

# Faucet (发放测试代币)
FAUCET_WALLET_PRIVATE_KEY=xxx    # Base58 格式，仅服务端

# 应用
NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app
```

### 安全文件 (永不提交)

```
.env.local                    # 环境变量
.private/                     # 私有目录
wallets/                      # 钱包文件
treasury-wallet.json          # Treasury 密钥
faucet-wallet.json            # Faucet 密钥
```

---

## 📊 文件统计

| 类型 | 数量 | 说明 |
|------|------|------|
| 组件 | ~15 | React 组件 |
| 服务 | 8 | 业务逻辑服务 |
| Hooks | 5 | 自定义 Hooks |
| 脚本 | 4 | 实用工具脚本 |
| 文档 | 12 | Markdown 文档 |
| 配置 | ~10 | 各种配置文件 |
| **总代码行** | **~2,500+** | 核心代码 |

---

## 🚀 性能优化

### 前端优化

1. **React 渲染优化**
   - `React.memo` 避免不必要重渲染
   - 自定义比较函数检查颜色和选中状态
   - `useCallback` 缓存事件处理函数

2. **Next.js 配置**
   - 包导入优化 (`optimizePackageImports`)
   - 生产环境移除 console.log
   - 静态资源长期缓存

3. **CSS 优化**
   - GPU 加速动画 (transform, opacity)
   - `prefers-reduced-motion` 支持

### 后端优化

1. **数据库索引**
   - 坐标查询索引
   - 所有者查询索引
   - 价格排序索引

2. **原子操作**
   - `FOR UPDATE` 行锁防止竞态条件
   - 事务隔离级别: READ COMMITTED

---

## 🎯 快速导航

### 新手阅读顺序

1. [README.md](./README.md) - 项目介绍
2. [docs/HACKATHON.md](./docs/HACKATHON.md) - 完整文档
3. [docs/TESTNET_SETUP.md](./docs/TESTNET_SETUP.md) - 设置指南

### 开发相关

- **组件**：`/components/game/`
- **支付逻辑**：`/lib/services/x402Payment.ts`
- **数据库**：`/supabase/schema-complete.sql`
- **国际化**：`/lib/i18n/`

### 部署相关

- **环境配置**：`.env.example`
- **数据库迁移**：`/supabase/`
- **Token 设置**：`/scripts/`
- **Vercel 配置**：`vercel.json`

---

## 🔄 维护建议

### 添加新功能

1. 组件 → `/components/game/`
2. 业务逻辑 → `/lib/services/`
3. API 端点 → `/app/api/`
4. 翻译文本 → `/lib/i18n/translations.ts`

### 更新文档

1. 主文档 → `docs/HACKATHON.md`
2. 专项文档 → `docs/` 对应文件
3. 项目结构 → `PROJECT_STRUCTURE.md`

### 数据库更改

1. 修改 → `/supabase/schema-complete.sql`
2. 测试 → Supabase Dashboard
3. 迁移 → 创建新的迁移文件

---

**最后更新**: 2026-01-26
